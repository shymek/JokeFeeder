package dev.szymion.remote.services

enum class EscapeOptions(val apiParameter: String) {
    JavaScript("javascript")
}

enum class FilterOptions(val apiParameter: String) {
    Explicit("explicit")
}
